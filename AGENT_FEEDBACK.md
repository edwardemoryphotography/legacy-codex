# AGENT_FEEDBACK.md

Append-only log. One improvement note per session, added at the end of that session (see `AGENTS.md` § RULES). Newest entry on top.

---

## 2026-07-12 — agent router / task queue / session rules setup

Note: The workspace had no shared entry point before this — five separate coordination concepts (leadership kit, triage, handoff log, task queue, router) were referenced conceptually but only `STATE.md` actually existed. Worth checking in future sessions whether `STATE.md` and `NEXT-ACTION-LOG.md` drift apart in purpose now that both exist; if so, fold one into the other rather than maintaining two similar logs.
