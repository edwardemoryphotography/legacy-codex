High focus (88%) favors prioritizing complex architectural work to leverage the current state for deep thinking.

## Backlog (re-ranked)

- [ ] Set `BIOMETRICS_SOURCE=file` and wire Muse bridge to `notes/biometric-state.json`
- [ ] Resolve Vercel API auth and run `npm run vercel-bridge` successfully
- [ ] One visible step from current `SHIPPING_BLOCKER.txt`

_To rebuild this file from voice/raw captures, add `notes/capture_*.md` files and run `npm run triage`._