# Daily startup — Legacy Codex v37 (local body)

Minimal sequence before doing real work. Skip steps that do not apply.

1. `cd ~/legacy-codex` (or open this folder in the editor).
2. Read `SHIPPING_BLOCKER.txt` — one sentence, current stall.
3. Open the newest file in `notes/` (or start from `TEMPLATE_SESSION_RESUME.md` if beginning a new session log).
4. If you are routing a capture: read `DELEGATION_RULES_v1.md` and assign **one** primary destination.
5. Execute **one** tiny step with a visible artifact (file edit, note, or checklist tick); avoid strategy spirals.

**When app behavior matters:** confirm against production at [legacy-codex.vercel.app](https://legacy-codex.vercel.app) (production truth wins).

**When editing the Codex app:** respect the FREEZE SPEC in `AGENTS.md` unless you explicitly escalate with `REWRITE THE APP CODE`.
